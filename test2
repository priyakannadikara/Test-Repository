import { Page } from "@playwright/test";
import { Locators } from "./types";

export default class HomePage {
  locators: Locators;
  constructor(public readonly page: Page) {
    this.locators = {
      homeTitle : this.page.locator('div h1[class*="Title"]'),
    };
  }
}
